% SB: These packages were chosen solely by me based on my requirements and preferences. For most packages, excepting
% babel, this should not affect university requirements.

% Encoding
\usepackage[utf8]{inputenc} % enables the use of UTF-8 as character encoding
\usepackage{microtype} % enables certain features 'towards typographical perfection'

\usepackage[T1]{fontenc} % ensures the use of font encodings that support accented characters

% Fonts
\usepackage{calrsfs}
\DeclareMathAlphabet{\pazocal}{OMS}{zplm}{m}{n}

% Graphics
\usepackage[pdftex]{graphicx}
\usepackage[usenames,dvipsnames]{xcolor}

% Tables
\usepackage{booktabs}
\usepackage{array}
\usepackage{rotating}

% Algorithms
\usepackage{listings}
\usepackage{algorithm}
\usepackage{algpseudocode}

% Others
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathptmx}
\usepackage{etoolbox}
\usepackage{url}
\usepackage{multirow}
\usepackage{multicol}
% \usepackage{textcomp}
\usepackage{footnote}
% SB: According to the hyperref documentation, the algorithm package should be loaded after hyperref
\usepackage[pdftex,breaklinks,bookmarksnumbered,linktocpage=false,hidelinks=true]{hyperref}
\usepackage[nameinlink,capitalise,noabbrev]{cleveref}

\usepackage{cite}
\usepackage{bm}
\usepackage{xparse}
\usepackage[section]{placeins}
\usepackage{lipsum}
\usepackage{subcaption}
\usepackage{soul}
\captionsetup[figure]{justification=centering}



\graphicspath{ {figs/} }

\newcolumntype{H}{>{\setbox0=\hbox\bgroup}c<{\egroup}@{}}

% THINGS I ADDED!
\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{3}
\usepackage{float}
\usepackage{titlesec}
\titleformat{\paragraph}[block]
{\normalfont\normalsize\bfseries\itshape}
% {\small\bfseries\itshape} % If you want the paragraph title font to be smaller
{\theparagraph}{1em}{}
\titlespacing*{\paragraph}{0pt}{2ex plus .4ex minus .2ex}{0.5ex}

\usepackage{indentfirst}

\usepackage{tabularx}
\usepackage{array}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{adjustbox}
\usepackage{tablefootnote}
\usepackage{threeparttable}
\usepackage{longtable}

\usepackage{etoolbox} % You might need this package for robust definitions

% This command combines \Cref* (type and number) and \nameref* (name/caption)
% inside a single \hyperref link.
\makeatletter
\newcommand*{\fullref}[1]{%
        \hyperref[{#1}]{\Cref*{#1}: \nameref*{#1}}%
}
\makeatother

% \makeatletter
% % 1. For Tables and Figures (uses the Caption, includes a colon)
% \newcommand*{\tabfigref}[1]{%
%         \hyperref[{#1}]{\Cref*{#1}: \nameref*{#1}}%
% }

% % 2. For Sections, Chapters, etc. (uses the Title, omits the colon for cleaner spacing)
% \newcommand*{\sectchapref}[1]{%
%   \hyperref[{#1}]{\Cref*{#1} \nameref*{#1}}%
% }

% \newcommand*{\sectchapref}[1]{%
%         \hyperref[{#1}]{\Cref*{#1} \getrefbykeydefault{#1}{name}{\@firstofone}}%
% }
% \makeatother

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "dissertation"
%%% End:
